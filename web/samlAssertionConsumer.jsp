<%//response.sendRedirect("/rcs/dashboard.xhtml");
//return;
    System.out.println("response.isCommitted(): " + response.isCommitted());
    //session.setAttribute("nameId", auth.getNameId());
    //com.onelogin.saml2.servlet.ServletUtils.sendRedirect(response, "/rcs/dashboard.xhtml");
    //RequestDispatcher dispatcher = getServletContext().getRequestDispatcher("/rcs/dashboard.xhtml");
    //dispatcher.forward(request, response);
    //ServletUtils.sendRedirect(response, "/rcs/dashboard.xhtml", null, false);
    //response.sendRedirect("/rcs/dashboard.xhtml");

    //return;
%>
